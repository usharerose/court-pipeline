repos:
  - repo: local
    hooks:
      - id: validate-commit-msg
        name: Validate commit message
        entry: scripts/validate-commit-msg.sh
        language: script
        stages: [commit-msg]
